open the project then open a terminal.
cd backend then use python -m venv venv after that use venv\Scripts\activate
the backend should start working on [localhost](http://localhost:3000/tasks)

open another terminal and cd vue-crud
run npm run dev
that will start the frontend on http://localhost:5173/

now its running have fun.
